<Window x:Class="WebViewHub.Controls.AppleMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="消息" Height="Auto" Width="300" SizeToContent="Height"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" Topmost="True"
        ResizeMode="NoResize">
    <Border Background="#f9f9f9" CornerRadius="12" BorderBrush="#e5e5e7" BorderThickness="1" SnapsToDevicePixels="True" Margin="10">
        <Border.Effect>
            <DropShadowEffect Color="#000000" Opacity="0.15" BlurRadius="20" ShadowDepth="4"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <!-- Title -->
            <TextBlock x:Name="TitleText" Text="提示" Grid.Row="0" HorizontalAlignment="Center" FontWeight="SemiBold" FontSize="16" Margin="0,20,0,8" Foreground="#1d1d1f"/>
            <!-- Message -->
            <TextBlock x:Name="MessageText" Text="Message" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="13" Foreground="#1d1d1f" TextWrapping="Wrap" Margin="24,0,24,20" TextAlignment="Center"/>
            
            <!-- Buttons -->
            <Border Grid.Row="2" BorderBrush="#dcdcdc" BorderThickness="0,1,0,0">
                <Grid x:Name="ButtonContainer" Height="44">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Border x:Name="Separator" Grid.Column="1" BorderBrush="#dcdcdc" BorderThickness="1,0,0,0" HorizontalAlignment="Left" Width="1"/>
                    
                    <Button x:Name="BtnCancel" Grid.Column="0" Content="取消" Click="BtnCancel_Click" Background="Transparent" BorderThickness="0" Foreground="#007aff" FontSize="16" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                    <Button x:Name="BtnOk" Grid.Column="1" Content="确定" Click="BtnOk_Click" Background="Transparent" BorderThickness="0" Foreground="#007aff" FontSize="16" FontWeight="SemiBold" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
